import { FormEvent, useCallback, useEffect, useState } from 'react'

type Page = 'overview' | 'accounts' | 'credits' | 'models' | 'automation' | 'scheduler' | 'activities' | 'oauth'
type Any = Record<string, unknown>

async function request<T>(url: string, init?: RequestInit): Promise<T> {
  const response = await fetch(url, { credentials: 'same-origin', headers: init?.body ? { 'Content-Type': 'application/json' } : undefined, ...init })
  const body = await response.json().catch(() => ({})) as Any
  if (!response.ok) throw new Error(String(body.error || '请求失败'))
  return body as T
}
const get = <T,>(url: string) => request<T>(url)
const post = <T,>(url: string, body: unknown = {}) => request<T>(url, { method: 'POST', body: JSON.stringify(body) })

export default function App() {
  const [authed, setAuthed] = useState<boolean | null>(null)
  const [page, setPage] = useState<Page>('overview')
  const [notice, setNotice] = useState('')

  const check = useCallback(async () => {
    try { const s = await get<{ authenticated: boolean }>('/api/session'); setAuthed(s.authenticated) } catch { setAuthed(false) }
  }, [])
  useEffect(() => { void check() }, [check])
  if (authed === null) return <div className="center">正在连接本地控制台…</div>
  if (!authed) return <Login onDone={check} />

  const nav: [Page, string, string][] = [
    ['overview', '概览', '⌁'], ['accounts', '账号管理', '◉'], ['credits', '积分管理', '＋'],
    ['models', '模型管理', '◇'], ['automation', '自动化管理', '◷'], ['scheduler', '账号云端定时任务', '☁'],
    ['activities', '活动管理', '✦'], ['oauth', '添加账号', '→'],
  ]
  return <div className="app-shell">
    <aside className="sidebar">
      <div className="wordmark"><b>WORK<br />BUDDY<span>›</span></b><small>LOCAL CONTROL ROOM</small></div>
      <nav>{nav.map(([id, label, icon]) => <button key={id} className={page === id ? 'nav active' : 'nav'} onClick={() => setPage(id)}><i>{icon}</i>{label}</button>)}</nav>
      <div className="side-bottom"><span>第三方本地控制台</span><button className="link" onClick={() => void post('/api/logout').then(() => setAuthed(false))}>退出登录</button></div>
    </aside>
    <main className="main">
      {notice && <div className="notice">{notice}<button onClick={() => setNotice('')}>×</button></div>}
      {page === 'overview' && <Overview onPage={setPage} />}
      {page === 'accounts' && <Accounts onNotice={setNotice} />}
      {page === 'credits' && <Credits />}
      {page === 'models' && <Models />}
      {page === 'automation' && <Automation onNotice={setNotice} />}
      {page === 'scheduler' && <Scheduler />}
      {page === 'activities' && <Activities />}
      {page === 'oauth' && <OAuth onNotice={setNotice} onDone={() => setPage('accounts')} />}
    </main>
  </div>
}

function Login({ onDone }: { onDone: () => Promise<void> }) {
  const [username, setUsername] = useState('admin'); const [password, setPassword] = useState(''); const [error, setError] = useState('')
  const submit = async (e: FormEvent) => { e.preventDefault(); try { await post('/api/login', { username, password }); await onDone() } catch (err) { setError(err instanceof Error ? err.message : '登录失败') } }
  return <div className="login"><div className="login-card"><div className="eyebrow">LOCAL CONTROL ROOM</div><h1>账号工作台</h1><p>只读取账号凭据并直接连接 WorkBuddy 上游。</p><form onSubmit={submit}><label>用户名<input value={username} onChange={e => setUsername(e.target.value)} autoComplete="username" /></label><label>密码<input value={password} onChange={e => setPassword(e.target.value)} type="password" autoFocus autoComplete="current-password" /></label>{error && <div className="error">{error}</div>}<button className="black" type="submit">进入控制台</button></form></div></div>
}

function Head({ eyebrow, title, text, action }: { eyebrow: string; title: string; text: string; action?: React.ReactNode }) { return <header className="head"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p>{text}</p></div>{action}</header> }
function Loading() { return <div className="loading">正在直接查询 WorkBuddy 上游…</div> }
function ErrorView({ text }: { text: string }) { return <div className="error card">{text}</div> }
function fmt(v: unknown) { const n = Number(v); return Number.isFinite(n) ? n.toLocaleString('zh-CN', { maximumFractionDigits: 2 }) : '—' }
function displayTime(v: unknown) { const raw = String(v || ''); return raw && !raw.startsWith('0001-') ? new Date(raw).toLocaleString() : '未执行' }

function Overview({ onPage }: { onPage: (p: Page) => void }) {
  const [data, setData] = useState<Any | null>(null); const [error, setError] = useState('')
  const load = useCallback(() => void get<Any>('/api/overview').then(setData).catch(e => setError(e.message)), [])
  useEffect(load, [load]); if (error) return <ErrorView text={error} />; if (!data) return <Loading />
  const cards = [['健康账号', data.healthy], ['账号总数', data.accounts], ['已过期', data.expired], ['已启用自动化', data.automations]]
  return <><Head eyebrow="LOCAL CONTROL ROOM" title="账号工作台" text="账号池状态、今日积分与已启用自动化" action={<button className="black" onClick={load}>刷新状态</button>} /><div className="stats">{cards.map(([label, value]) => <div className="stat" key={String(label)}><span>{String(label)}</span><b>{String(value)}</b></div>)}</div><div className="grid two"><section className="card"><h2>快速入口</h2><div className="quick"><button onClick={() => onPage('credits')}>查看今日积分汇总 →</button><button onClick={() => onPage('automation')}>管理本地自动化 →</button><button onClick={() => onPage('scheduler')}>查看账号云端定时任务 →</button><button onClick={() => onPage('activities')}>探测新活动任务 →</button></div></section><section className="card"><h2>运行边界</h2><p className="muted">此面板不会读取或改写 workbuddy2api 的配置，也不会调用 Docker 或重启网关。凭据仅保存在共享的 auths 目录。</p></section></div></>
}

function Accounts({ onNotice }: { onNotice: (s: string) => void }) { const [items,setItems]=useState<Any[]>([]);const [error,setError]=useState('');const load=useCallback(()=>void get<{accounts:Any[]}>('/api/accounts').then(d=>setItems(d.accounts)).catch(e=>setError(e.message)),[]);useEffect(load,[load]);const action=async(uid:string,a:string)=>{try{const r=await post<{message:string}>(`/api/accounts/${encodeURIComponent(uid)}/actions/${a}`);onNotice(r.message);load()}catch(e){onNotice(e instanceof Error?e.message:'执行失败')}};if(error)return <ErrorView text={error}/>;return <><Head eyebrow="ACCOUNTS" title="账号管理" text="OAuth 添加、凭据刷新与单账号操作" action={<button className="black" onClick={load}>刷新</button>}/><section className="card table"><table><thead><tr><th>账号</th><th>区域</th><th>Token</th><th>操作</th></tr></thead><tbody>{items.map(a=><tr key={String(a.uid)}><td><b>{String(a.nickname || a.uid)}</b><small>{String(a.uid)}</small></td><td>{String(a.domain || 'cn')}</td><td><Badge ok={!a.expired}>{a.expired?'已过期':a.needs_refresh?'即将过期':'正常'}</Badge></td><td><button onClick={()=>void action(String(a.uid),'refresh')}>刷新凭据</button><button onClick={()=>void action(String(a.uid),'checkin')}>签到</button><button onClick={()=>void action(String(a.uid),'travel')}>旅行巡检</button></td></tr>)}</tbody></table>{items.length===0&&<Empty text="尚无账号，请先通过 OAuth 添加账号。"/>}</section></> }

function Credits(){const [items,setItems]=useState<Any[]|null>(null);const [error,setError]=useState('');const [detail,setDetail]=useState<Any|null>(null);const load=useCallback(()=>void get<{items:Any[]}>('/api/credits').then(d=>setItems(d.items)).catch(e=>setError(e.message)),[]);useEffect(load,[load]);if(error)return <ErrorView text={error}/>;if(!items)return <Loading/>;return <><Head eyebrow="CREDITS" title="积分管理" text="默认展示今日额度发放、已用、剩余；不建立本地积分账本。" action={<button className="black" onClick={load}>查询上游</button>}/><section className="card table"><table><thead><tr><th>账号</th><th>当前积分</th><th>今日发放额度</th><th>今日已用</th><th>今日剩余</th><th></th></tr></thead><tbody>{items.map(x=><tr key={String(x.uid)}><td>{String(x.nickname||x.uid)}</td><td><b>{fmt(x.current)}</b></td><td>{fmt(x.today_allocated)}</td><td>{fmt(x.today_consumed)}</td><td>{fmt(x.today_remaining)}</td><td><button onClick={()=>void get<{items:Any[]}>(`/api/credits/${encodeURIComponent(String(x.uid))}`).then(d=>setDetail(d.items[0]||null))}>查看明细</button></td></tr>)}</tbody></table></section><p className="muted">“今日发放/已用/剩余”来自上游免费套餐的当日切片，并非虚构的积分交易流水。</p>{detail&&<div className="drawer"><button className="close" onClick={()=>setDetail(null)}>×</button><h2>{String(detail.nickname)} 的今日额度明细</h2><dl><dt>当前积分</dt><dd>{fmt(detail.current)}</dd><dt>套餐数</dt><dd>{fmt(detail.packages)}</dd><dt>查询时间</dt><dd>{new Date(String(detail.fetched_at)).toLocaleString()}</dd></dl>{Boolean(detail.error)&&<div className="error">{String(detail.error)}</div>}</div>}</>}

function Models(){const [items,setItems]=useState<Any[]|null>(null);const [error,setError]=useState('');const load=useCallback(()=>void get<{items:Any[]}>('/api/models').then(d=>setItems(d.items)).catch(e=>setError(e.message)),[]);useEffect(load,[load]);if(error)return <ErrorView text={error}/>;if(!items)return <Loading/>;return <><Head eyebrow="MODELS" title="模型管理" text="按账号直连上游查询可用模型，不依赖网关模型列表。" action={<button className="black" onClick={load}>刷新模型</button>}/><section className="card table"><table><thead><tr><th>账号</th><th>模型 ID</th><th>名称</th><th>状态</th></tr></thead><tbody>{items.map((m,i)=><tr key={`${String(m.uid)}-${i}`}><td>{String(m.nickname||m.uid)}</td><td className="mono">{String(m.id||'—')}</td><td>{String(m.name||'—')}</td><td>{m.error?<Badge ok={false}>{String(m.error)}</Badge>:<Badge ok>来自上游</Badge>}</td></tr>)}</tbody></table></section></>}

function Automation({onNotice}:{onNotice:(s:string)=>void}){const [items,setItems]=useState<Any[]|null>(null);const [runs,setRuns]=useState<Any[]>([]);const [error,setError]=useState('');const [intervals,setIntervals]=useState<Record<string,string>>({});const load=useCallback(()=>void get<{items:Any[];runs:Any[]}>('/api/automations').then(d=>{setItems(d.items);setRuns(d.runs);setIntervals(Object.fromEntries(d.items.map(a=>[String(a.id),String(a.every_minutes)])))}).catch(e=>setError(e.message)),[]);useEffect(load,[load]);const every=(a:Any)=>Number(intervals[String(a.id)]||a.every_minutes);const save=async(a:Any,enabled:boolean)=>{try{await request(`/api/automations/${encodeURIComponent(String(a.id))}`,{method:'PUT',body:JSON.stringify({enabled,every_minutes:every(a)})});onNotice(`${String(a.name)}配置已保存`);load()}catch(e){onNotice(e instanceof Error?e.message:'保存失败')}};const run=async(a:Any)=>{try{const r=await post<{message:string}>(`/api/automations/${encodeURIComponent(String(a.id))}/run`);onNotice(r.message);load()}catch(e){onNotice(e instanceof Error?e.message:'执行失败')}};if(error)return <ErrorView text={error}/>;if(!items)return <Loading/>;return <><Head eyebrow="AUTOMATION" title="自动化管理" text="这些是真实持久化的面板定时任务。默认仅开启新活动探测，避免与网关既有任务双跑。"/><section className="card"><div className="automation-list">{items.map(a=><div className="automation" key={String(a.id)}><div><h2>{String(a.name)}</h2><p>上次：{displayTime(a.last_run_at)}</p><small>{String(a.last_result||'等待执行')}</small></div><div className="actions"><label className="interval">每 <input aria-label={`${String(a.name)}执行间隔`} type="number" min="5" max="10080" value={intervals[String(a.id)]||String(a.every_minutes)} onChange={e=>setIntervals(v=>({...v,[String(a.id)]:e.target.value}))}/> 分钟</label><button onClick={()=>void save(a,Boolean(a.enabled))}>保存配置</button><button className={a.enabled?'switch on':'switch'} aria-label={`切换${String(a.name)}`} onClick={()=>void save(a,!a.enabled)}><span/></button><button onClick={()=>void run(a)}>立即运行</button></div></div>)}</div></section>{runs.length>0&&<section className="card"><h2>最近运行</h2>{runs.slice(0,8).map((r,i)=><p className="run" key={i}><Badge ok={Boolean(r.ok)}>{r.ok?'成功':'失败'}</Badge> {String(r.action)} · {String(r.message)}</p>)}</section>}</>}

function Scheduler(){
  const [items,setItems]=useState<Any[]|null>(null); const [mock,setMock]=useState<Any[]|null>(null); const [accounts,setAccounts]=useState<Any[]>([]); const [note,setNote]=useState(''); const [detail,setDetail]=useState<Any|null>(null); const [error,setError]=useState(''); const [draft,setDraft]=useState({account_uid:'',name:'',cron:'0 9 * * *',prompt:'',enabled:true}); const [edits,setEdits]=useState<Record<string,Any>>({})
  const load=useCallback(()=>void Promise.all([get<{items:Any[];note:string}>('/api/scheduler-tasks'),get<{items:Any[];note:string}>('/api/mock-scheduler-tasks'),get<{accounts:Any[]}>('/api/accounts')]).then(([up,local,acc])=>{setItems(up.items);setMock(local.items);setNote(`${up.note} ${local.note}`);setAccounts(acc.accounts);setDraft(v=>v.account_uid?v:(acc.accounts[0]?{...v,account_uid:String(acc.accounts[0].uid)}:v));setEdits(Object.fromEntries(local.items.map(t=>[String(t.id),t])))}).catch(e=>setError(e.message)),[])
  useEffect(load,[load])
  const inspect=(x:Any)=>void get<{item:Any}>(`/api/scheduler-tasks/${encodeURIComponent(String(x.uid))}/${encodeURIComponent(String(x.id))}`).then(d=>setDetail(d.item)).catch(e=>setError(e.message))
  const create=async()=>{try{await post('/api/mock-scheduler-tasks',draft);setDraft(v=>({...v,name:'',prompt:''}));load()}catch(e){setError(e instanceof Error?e.message:'创建 Mock 任务失败')}}
  const update=async(id:string)=>{try{const task=edits[id];await request(`/api/mock-scheduler-tasks/${encodeURIComponent(id)}`,{method:'PUT',body:JSON.stringify(task)});load()}catch(e){setError(e instanceof Error?e.message:'保存 Mock 任务失败')}}
  const remove=async(id:string)=>{try{await request(`/api/mock-scheduler-tasks/${encodeURIComponent(id)}`,{method:'DELETE'});load()}catch(e){setError(e instanceof Error?e.message:'删除 Mock 任务失败')}}
  if(error)return <ErrorView text={error}/>; if(!items||!mock)return <Loading/>
  return <><Head eyebrow="ACCOUNT SCHEDULER · MOCK" title="账号云端定时任务" text="上游 scheduler 未授权时，使用本地 Mock 骨架演练任务管理；不会向 WorkBuddy 创建或修改任何任务。" action={<button className="black" onClick={load}>刷新任务</button>}/><div className="notice neutral">{note}</div><section className="card mock-form"><h2>创建 Mock 云端任务</h2><div className="mock-fields"><label>账号<select value={draft.account_uid} onChange={e=>setDraft(v=>({...v,account_uid:e.target.value}))}>{accounts.map(a=><option key={String(a.uid)} value={String(a.uid)}>{String(a.nickname||a.uid)}</option>)}</select></label><label>任务名称<input value={draft.name} placeholder="例如：每日工作摘要" onChange={e=>setDraft(v=>({...v,name:e.target.value}))}/></label><label>调度表达式<input value={draft.cron} onChange={e=>setDraft(v=>({...v,cron:e.target.value}))}/></label><label>任务内容<textarea value={draft.prompt} placeholder="Mock 内容，仅本地保存" onChange={e=>setDraft(v=>({...v,prompt:e.target.value}))}/></label><label className="check"><input type="checkbox" checked={draft.enabled} onChange={e=>setDraft(v=>({...v,enabled:e.target.checked}))}/> 启用</label></div><button className="black" disabled={!draft.account_uid||!draft.name.trim()} onClick={()=>void create()}>创建 Mock 任务</button></section><section className="card table"><h2>本地 Mock 任务</h2><p className="muted">仅用于先完成页面与交互骨架，数据保存在控制台自己的 state.json。</p><table><thead><tr><th>账号</th><th>名称 / 调度</th><th>内容</th><th>状态</th><th>操作</th></tr></thead><tbody>{mock.map(t=>{const e=edits[String(t.id)]||t;return <tr key={String(t.id)}><td>{String(t.nickname||t.account_uid)}</td><td><input value={String(e.name||'')} onChange={x=>setEdits(v=>({...v,[String(t.id)]:{...e,name:x.target.value}}))}/><input value={String(e.cron||'')} onChange={x=>setEdits(v=>({...v,[String(t.id)]:{...e,cron:x.target.value}}))}/></td><td><textarea value={String(e.prompt||'')} onChange={x=>setEdits(v=>({...v,[String(t.id)]:{...e,prompt:x.target.value}}))}/></td><td><label className="check"><input type="checkbox" checked={Boolean(e.enabled)} onChange={x=>setEdits(v=>({...v,[String(t.id)]:{...e,enabled:x.target.checked}}))}/> {e.enabled?'启用':'停用'}</label></td><td><button onClick={()=>void update(String(t.id))}>保存</button><button onClick={()=>void remove(String(t.id))}>删除</button></td></tr>})}</tbody></table>{mock.length===0&&<Empty text="尚无 Mock 云端任务。请先创建一条本地骨架任务。"/>}</section><section className="card table"><h2>上游真实任务（只读）</h2><table><thead><tr><th>账号</th><th>任务</th><th>状态</th><th>更新时间</th><th></th></tr></thead><tbody>{items.map((x,i)=><tr key={`${String(x.uid)}-${i}`}><td>{String(x.nickname||x.uid)}</td><td>{String(x.name||x.id||'—')}</td><td>{x.error?<Badge ok={false}>{String(x.error)}</Badge>:<Badge ok={String(x.status).toLowerCase()!=='failed'}>{String(x.status||'未知')}</Badge>}</td><td>{String(x.updated||'—')}</td><td>{Boolean(x.id)&&<button onClick={()=>inspect(x)}>查看详情</button>}</td></tr>)}</tbody></table>{items.length===0&&<Empty text="上游暂未返回任何账号云端定时任务。"/>}</section>{detail&&<div className="drawer"><button className="close" onClick={()=>setDetail(null)}>×</button><h2>账号云端任务详情</h2><pre>{JSON.stringify(detail,null,2)}</pre></div>}</>}

function Activities(){const [items,setItems]=useState<Any[]|null>(null);const load=useCallback(()=>void get<{items:Any[]}>('/api/activities').then(d=>setItems(d.items)),[]);useEffect(load,[load]);if(!items)return <Loading/>;return <><Head eyebrow="ACTIVITIES" title="活动管理" text="直接读取成长任务，自动化探测以任务编号去重发现新增任务。" action={<button className="black" onClick={load}>立即探测</button>}/><section className="card table"><table><thead><tr><th>账号</th><th>活动任务</th><th>状态</th><th>奖励</th><th></th></tr></thead><tbody>{items.map((x,i)=><tr key={`${String(x.uid)}-${String(x.code)}-${i}`}><td>{String(x.nickname||x.uid)}</td><td>{String(x.name||x.code)}</td><td>{String(x.status||'—')}</td><td>{fmt(x.reward)}</td><td>{Boolean(x.new)&&<Badge ok>新发现</Badge>}</td></tr>)}</tbody></table>{items.length===0&&<Empty text="当前没有可读取的成长任务。"/>}</section></>}

function OAuth({onNotice,onDone}:{onNotice:(s:string)=>void;onDone:()=>void}){const[region,setRegion]=useState('cn');const[flow,setFlow]=useState<{id:string;url:string}|null>(null);const[starting,setStarting]=useState(false);const start=async()=>{setStarting(true);try{setFlow(await post('/api/oauth/start',{region}) as {id:string;url:string})}catch(e){onNotice(e instanceof Error?e.message:'发起失败')}finally{setStarting(false)}};useEffect(()=>{if(!flow)return;const timer=window.setInterval(()=>void post<{status:string;nickname?:string}>(`/api/oauth/${encodeURIComponent(flow.id)}/poll`).then(r=>{if(r.status==='success'){window.clearInterval(timer);onNotice(`账号 ${r.nickname||''} 已保存`);onDone()}}).catch(()=>undefined),3000);return()=>window.clearInterval(timer)},[flow,onDone,onNotice]);return <><Head eyebrow="OAUTH" title="添加账号" text="选择账号区域后完成 WorkBuddy 网页授权；凭据只写入共享 auths 目录。"/><section className="card oauth"><label>账号区域<select value={region} onChange={e=>setRegion(e.target.value)}><option value="cn">中国大陆</option><option value="global">国际版</option></select></label><button className="black" disabled={starting} onClick={()=>void start()}>{starting?'正在获取授权链接…':'发起 OAuth 授权'}</button>{flow&&<div className="oauth-link"><p>请在新窗口完成授权，控制台会自动轮询结果。</p><a className="black" href={flow.url} target="_blank" rel="noreferrer">打开 WorkBuddy 授权页</a></div>}</section></>}

function Badge({ok,children}:{ok?:boolean;children:React.ReactNode}){return <span className={ok===false?'badge bad':'badge'}>{children}</span>};function Empty({text}:{text:string}){return <div className="empty">{text}</div>}
